{
  "name": "@contrail/skills",
  "version": "0.1.0",
  "description": "Contrail bundled skill pack — the Contrail-adapted Salesforce skills shipped with the desktop app, plus the frontmatter reader the skill library uses.",
  "license": "UNLICENSED",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "sync": "node scripts/sync-skills.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
